# project

## Run On Android Emulator

From `FE_MyTime`, run:

```powershell
.\run-android.ps1
```

or:

```cmd
run-android.cmd
```

These scripts automatically set the Flutter environment flags needed on this
machine so you do not have to type them every time.

## Getting Started

This is a Flutter project.
